{% extends 'base.html.twig' %}

{% block title %}Concept du Midi{% endblock %}

{% block body %}

    <div class="container mx-auto">

        <h1>Concept du Midi</h1>

        <div class="">
            {% for texte in textes %}
            {% if texte.etat == true %}
            <div style="padding-top: 1rem">
                <span>{{ texte.description | raw }}</span>
            </div>
        </div>
        <hr>

        <div class="text-center pt-5">
            {% if images is not empty %}
                <div id="carouselExampleIndicators" class="carousel slide p-3 " data-bs-ride="carousel">
                    <div class="carousel-inner">
                        {% for images in images %}
                            <div class="carousel-item {% if loop.first %} active {% endif %}">
                                <img src="{{ asset('asset/images/' ~ images.imageName) }}"
                                     class="d-block w-100 h-100 rounded-5" alt="...">
                            </div>
                        {% endfor %}
                    </div>
                </div>
            {% endif %}
            {% endif %}
            {% endfor %}
        </div>


    </div>


    {#    <div class="mx-auto"> #}

    {#        <h1>Plats du midi Salés</h1> #}
    {#        {% for platsSale in platsSales %} #}

    {#            {% if loop.index %2==1 or loop.first %} #}
    {#                <div class="row justify-content-center"> #}
    {#            {% endif %} #}

    {#            <div class="col-sm-12 col-md-3 d-flex align-items-stretch"> #}
    {#                <div class="card d-flex text-white bg-primary mb-3"> #}
    {#                    <div class="card-header"> #}
    {#                        <h2>{{ platsSale.titre | raw }}</h2> #}
    {#                    </div> #}
    {#                    <div class="card-body">{{ platsSale.description | raw }}</div> #}
    {#                    {% if imagesPlatsSales is not empty %} #}
    {#                        <div class="card-img"> #}
    {#                            <div id="carouselExampleIndicators" class="carousel slide p-3 " data-bs-ride="carousel"> #}
    {#                                <div class="carousel-inner"> #}

    {#                                    {% for imagesPlatsSale in platsSale.imagesPlatsSales %} #}

    {#                                        <div class="carousel-item {% if loop.first %} active {% endif %}"> #}
    {#                                            <img src="{{ asset('asset/images/' ~ imagesPlatsSale.imageName) }}" #}
    {#                                                 class="d-block w-100 h-100 rounded-2" alt="images plats"> #}
    {#                                        </div> #}

    {#                                    {% endfor %} #}
    {#                                </div> #}
    {#                            </div> #}
    {#                        </div> #}
    {#                    {% endif %} #}
    {#                </div> #}
    {#            </div> #}
    {#            {% if loop.index %2==0 or loop.last %} #}
    {#                </div> #}
    {#            {% endif %} #}
    {#        {% endfor %} #}
    {#    </div> #}


    {#    <div class="mx-auto"> #}

    {#        <h1>Plats du midi Sucrés</h1> #}

    {#        {% for platsSucre in platsSucres %} #}

    {#            {% if loop.index %2==1 or loop.first %} #}
    {#                <div class="row d-flex justify-content-center"> #}
    {#            {% endif %} #}

    {#            <div class="col-sm-12 col-md-3 d-flex align-items-stretch"> #}
    {#                <div class="card text-white bg-primary mb-3"> #}
    {#                    <div class="card-header"> #}
    {#                        <h2>{{ platsSucre.titre | raw }}</h2> #}
    {#                    </div> #}
    {#                    <div class="card-body">{{ platsSucre.description | raw }}</div> #}
    {#                    {% if imagesPlatsSucres is not empty %} #}
    {#                        <div class="card-img"> #}
    {#                            <div id="carouselExampleIndicators" class="carousel slide p-3 " data-bs-ride="carousel"> #}
    {#                                <div class="carousel-inner"> #}

    {#                                    {% for imagesPlatsSucre in platsSucre.imagesPlatsSucres %} #}

    {#                                        <div class="carousel-item {{ (loop.first ) ? 'active' : '' }}"> #}
    {#                                            <img src="{{ asset('asset/images/' ~ imagesPlatsSucre.imageName) }}" #}
    {#                                                 class="d-block w-100 h-100 rounded-2" alt="..."> #}
    {#                                        </div> #}

    {#                                    {% endfor %} #}
    {#                                </div> #}
    {#                            </div> #}
    {#                        </div> #}
    {#                    {% endif %} #}
    {#                </div> #}
    {#            </div> #}
    {#            {% if loop.index %2==0 or loop.last %} #}
    {#                </div> #}
    {#            {% endif %} #}
    {#        {% endfor %} #}
    {#    </div> #}


    <div class="row">
        <div class="liens">
            <h2>
                <a class="btn btn-primary" href="{{ path('reservation_midi') }}">Réserver</a>
            </h2>
            <h2>
                <a class="btn btn-primary" href="{{ path('concept_general') }}">Retour aux Concepts</a>
            </h2>
        </div>
    </div>

{% endblock %}
